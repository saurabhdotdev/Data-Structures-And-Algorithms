"""
1)Two Sum
Link: https://leetcode.com/problems/two-sum/
Approach: Use a hash map to track visited numbers.
Time: O(n), Space: O(n)
"""

def two_sum(nums, target):
    seen = {}
    for i, val in enumerate(nums):
        need = target - val
        if need in seen:
            return [seen[need], i]
        seen[val] = i
    return None


